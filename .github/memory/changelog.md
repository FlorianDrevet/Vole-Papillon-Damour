# Memory Changelog

| Date | Change |
|------|--------|
| 2026-09-11 | Simplified the Catalog administrator header access: removed the duplicated `Espace administrateur` links from desktop/mobile navigation, made the visible `Administration` tag route to `/administration`, and aligned it to the account trigger's 42px height. Validation: TDD red/green, 155 Catalog ChromeHeadless tests, SSR/browser build with the existing initial-budget warning, Graphify refresh, and `git diff --check`; no deployment was made. |
| 2026-09-11 | Resolved PR #134 conflicts against `origin/main` (`3a96844`): retained the main Catalog administration corrections and the PR's automatic session restore/direct Work/Edition follow journey, including both search regressions. Validation: `npm ci`, 150 Catalog ChromeHeadless tests, production build with the existing 500 kB initial-budget warning, Graphify refresh, and `git diff --check`; GitHub CI was not awaited and no deployment was made. |
| 2026-09-11 | Fixed the hosted External ID authentication background regression: the shared sign-in/sign-up CSS now applies an opaque paper-blue canvas and explicitly clears `background-image`, so the previously uploaded book-slice/stat bars no longer render behind the authentication card. Updated the Entra runbook to reapply branding without `-BackgroundImagePath`; no tenant mutation or deployment was made. The static CSS regression passes, Graphify was refreshed, and Pester remains blocked locally by the missing Microsoft Graph DirectoryManagement module. |
| 2026-09-11 | Polished the Catalog member journey: browser bootstrap now restores a cached MSAL account automatically, connected/admin header controls keep a stable width, recent home availability uses colored tags, and search external references use direct typed Work/Edition follow actions instead of a scope modal. The search filters and section rules were spaced/aligned to the Catalog visual language. Validation: 147 Catalog ChromeHeadless tests, production build with the existing 500 kB initial-budget warning, Graphify refresh, and local mobile browser smoke; no deployment. |
| 2026-09-11 | Resolved the Catalog conflicts for PR #129 against the latest `origin/main` (`b359829`): retained the main administration/Scan changes and the shared `BookCardComponent` `list` result layout while adding the PR's `home` variant and home sections. Added a regression for the combined list/home contract. Validation: 139 Catalog tests, production build, Graphify update/query, `git diff --check`, and local Chrome checks at 1280/390px without horizontal overflow; the local API was unavailable for data-backed card rendering, no deployment was made, and PR [#129](https://github.com/FlorianDrevet/Vole-Papillon-Damour/pull/129) remains open. |
| 2026-09-11 | Aligned the Catalog home with the book-fair mockup: removed `Votre sélection`, added the API-driven genre browser and account-following guidance after rare books, and added a dedicated compact recent-book card variant while preserving the default cover placeholder. Validation: 128 Catalog tests, production build, Graphify update, and desktop/mobile local mock-API checks; PR [#129](https://github.com/FlorianDrevet/Vole-Papillon-Damour/pull/129) open. |
| 2026-09-10 | Resolved the merge conflicts for PR #113 from fresh `origin/main` (`31fcead`), retaining the main alert hierarchy and camera-frame layout alongside the Scan reprise/status/diagnostic flows and the backend/BackOffice administration changes. Validation passes with 165 Scan ChromeHeadless tests, 349 backend tests, 5 BackOffice bootstrap tests, and the Scan, BackOffice, and backend builds; no deployment was made. |
| 2026-09-09 | Delivered Scanette reprise lots 0-5 in one branch: diagnostics/recovery, IndexedDB v3 and explicit set-aside states, session/account-switch integrity, routed Scan shell and confirmations, local set-aside telemetry, stale-session administration with Administration-only force-close, and delayed-alert documentation. Validation passes with 159 Scan, 18 BackOffice, and 343 backend tests plus both Angular builds; responsive/device checks remain manual. |
| 2026-09-10 | Polished the Catalog header: the external association link now aligns to the 42px navigation controls and gets the shared orange hover/focus underline, while the signed-out « Mon compte » trigger no longer renders an empty blue circle. Validation passes with 122 ChromeHeadless tests, the Catalog SSR/browser build, Graphify, and 1200/390px responsive browser checks; no deployment was made. |
| 2026-09-10 | Clarified the Website gallery contract: the 2004–2010 album now states that its three imported images are only a selection of the announced 231, while the video cards stretch to the same row height and keep their text/download areas aligned. Website validation passes 70 ChromeHeadless tests, the production build, and a local desktop browser smoke; no deployment was made. |
| 2026-09-10 | Refined the Website family gallery: album cards now open an accessible image selection, the mixed video grid no longer stretches horizontal cards, the hero has a three-image mosaic, the Maxence history CTA closes the page, and the Books Fair album is last. Website validation passes 70 ChromeHeadless tests, a clean `npm ci`, and the production build; responsive browser checks passed locally and no deployment was made. |
| 2026-09-09 | Integrated the unread Family Drevet editorial/media batch: the Website gallery now presents six curated photo albums and fourteen lazy-loaded video cards, the school narrative continues through 2026, and the transplant page covers both multivisceral and corneal grafts. Website validation passes 67 ChromeHeadless tests, the production build, and local browser smoke; no deployment was made. |
| 2026-09-09 | Improved Catalog `/prochaines-dates`: the client now reads the public `/asso-events` collection and filters all upcoming Books events, the next edition remains highlighted while the full schedule is listed below, Website calendar/clock/location assets are reused, and the Google Maps iframe is loaded only after dedicated Maps consent with an external-link fallback. PR #110 is open; no deployment was made. |
| 2026-09-09 | Reworked the Scan PWA login surface from the former centered card to the catalog-aligned 1a marine welcome screen and 1c missing-role state, including the butterfly asset, account identity, accessible error/actions, and responsive styling. Scan validation passes with 134 ChromeHeadless tests, the bootstrap contract, and the production build. |
| 2026-09-09 | Ajusté le démarrage de la Scanette : la restauration de session attend la fin de l’initialisation ou du redirect MSAL (`InteractionStatus.None`), tandis que la vérification silencieuse du jeton et des rôles ne bloque plus le bouton de connexion. Une interaction MSAL déjà en cours reste bloquante pour éviter les appels interactifs concurrents ; la suite Scan de 134 tests passe. |
| 2026-09-09 | Refined the hosted External ID maquette 1a: added 16px input padding, a neutral light-blue book-slice fallback behind the centered card, and replaced the flashing butterfly background asset on `0` and `fr-FR`; Graph background deletion returned 405 for this CIAM tenant, so the replacement is intentional. Pester passes 9/9 and no account or password was submitted. |
| 2026-09-09 | Aligned the public Catalog home hero with the exported Bandeau Accueil maquette 2a: the CSS book composition is replaced by the official butterfly and halo, the search keeps its typed genre/count behavior, and the next-fair information becomes a compact footer row with a « Plus d’infos » link. Catalog validation passes with 117 ChromeHeadless tests and the SSR/browser build; the existing initial bundle-budget warning remains and no deployment was made. |
| 2026-09-08 | Applied the hosted External ID branding V2 to tenant `b23c80b3-9776-4840-8255-fcbf3b3500fd`: Catalog blue/orange gradient theme, rounded authentication card, accessible focus/error states, dark footer, responsive rules, and the Catalog butterfly background are active on default `0` and `fr-FR`; live signup/sign-in smoke shows the complete visual theme, with 8 Pester tests passing and no account or password submitted. |
| 2026-09-08 | Implemented Catalog Lot 6 (`C-02`, `C-04`, `C-05`, `C-06`, `C-07`, `C-16`): public search now filters/sorts/pages in SQL with page-scoped related data and no full-table fallback; unknown routes render a noindex 404 with the correct SSR status; book SEO nodes are cleaned up on destruction; Fetch-based HTTP hydration and lazy MSAL initialization are enabled; and the dedicated `/asso-events/next-books` endpoint replaces the legacy catalog fair route. Validation passes with 122 Scan tests, 110 Catalog tests, 333 backend solution tests, and the Catalog SSR build; local 404/200 SSR smoke is green, with the existing bundle-budget warning. No deployment was made. |
| 2026-09-08 | Applied the hosted External ID maquette 1a on tenant `b23c80b3-9776-4840-8255-fcbf3b3500fd`: light paper-blue canvas, centered card, Catalog top accent, butterfly beside the title, orange actions and clear footer are active on `0` and `fr-FR` for signup and sign-in. Graph CSS readback matches the local stylesheet, Pester remains 8/8, and no account or password was submitted. |
| 2026-09-08 | Applied the hosted External ID branding V2 to tenant `b23c80b3-9776-4840-8255-fcbf3b3500fd`: Catalog blue/orange gradient theme, rounded authentication card, accessible focus/error states, dark footer, responsive rules, and the Catalog butterfly background are active on default `0` and `fr-FR`; live signup/sign-in smoke shows the complete visual theme, with 8 Pester tests passing and no account or password submitted. |
| 2026-09-08 | Completed the Catalog legal/analytics rollout after PR #94 (`e232d0f`): Catalog deploy runs `34165470413` and `34165490064` succeeded; live smoke verified the five legal routes, no audience scripts before consent, Clarity `yerabb7gnt` and GA4 `G-GBHC67EGGF` after consent, and working withdrawal. Submitted `https://livres.volepapillondamour.fr/sitemap.xml` to the existing domain Search Console property; Google reports “Opération effectuée” with 3 discovered pages. The next checks are delayed Clarity/GA4 data and the next sitemap crawl. |
| 2026-09-07 | Applied External ID branding to tenant `b23c80b3-9776-4840-8255-fcbf3b3500fd`: default localization `0` and `fr-FR` CSS streams match the repository stylesheet, the final script execution is clean, and Catalog deployment `34162378838` plus public signup/sign-in visual smoke are green. No account or password was submitted. |
| 2026-09-07 | Fixed first-run External ID branding: the script handles an absent fresh-tenant localization during `-WhatIf`, updates the External ID default localization `0`, uploads CSS to both `0` and `fr-FR`, and treats Graph write failures as fatal; six Pester branding tests pass. |
| 2026-09-07 | Added the Catalog legal/privacy/cookie/accessibility pages, Azure Container Apps hosting details, account/watchlist/alert disclosures, and an explicit consent banner. Microsoft Clarity and GA4 load only after audience consent; the Catalog workflow injects dedicated public IDs at build time. Created Clarity project `yerabb7gnt` and GA4 measurement stream `G-GBHC67EGGF`, while preserving the Website measurement variable `G-D67DMFCTDG`. Validation passes with 103 Catalog ChromeHeadless tests, the SSR/production build, and local legal-page smoke checks. |
| 2026-09-07 | Added Scan observability in PR #90: Azure Monitor/OpenTelemetry spans and low-cardinality duration metrics separate API metadata resolution, BnF/fallback providers, and SQL scan persistence; browser correlation, service naming, a >3 s metadata alert, Kusto diagnostics, and regression tests are included. Backend/Scan/Bicep validation passes; Azure deployment and live verification remain pending. |
| 2026-09-07 | Adapted the Catalog footer to its own journeys: catalogue search, genres, upcoming dates, research list, the association handoff, and local legal pages now replace the Website's Maxence/association-content links. Added the catalogue-specific privacy note and responsive three-column layout; 87 Catalog ChromeHeadless tests, the production build, and 1905/390px browser checks pass. |
| 2026-09-07 | Corrected the External ID public signup flow so ordinary French `displayName` values such as `Florian Drevet` are not rejected by an incompatible regex, including when updating the already-created flow; added French `ui_locales`/`mkt` parameters to Catalog MSAL requests, idempotent tenant branding/CSS provisioning, and responsive branding documentation. Validation passes with 84 Catalog ChromeHeadless tests, Catalog build, 5 user-flow Pester tests, and 4 branding Pester tests; tenant configuration and Catalog deployment remain pending after merge. |
| 2026-09-07 | **Refonte du parcours Catalog.** Ajout d'un onglet `Accueil` comme entrée du site avec teaser compact de la prochaine bourse aux livres, livres récents, livres rares et genres ; `/prochaines-dates` est désormais dédié aux détails complets du prochain rendez-vous. Le papillon isolé du hero est remplacé par une composition CSS de livres et les libellés fixes emploient « bourse aux livres ». Validation : 85 tests ChromeHeadless et build Catalog passants ; avertissement de budget initial Angular connu. |
| 2026-09-07 | Added idempotent `infra/entra/Configure-EntraUserFlow.ps1` provisioning for the External ID email/password signup form, restricted to the public Catalog app, with Pester coverage for the Graph payload and `-WhatIf`; updated the identity/auth documentation. No tenant mutation or deployment was performed. |
| 2026-09-07 | Refined the Catalog home hero: removed the background grid and orbit decoration, replaced it with the official association butterfly, clarified the title as « bourse aux livres », removed the duplicate fair date and search-button arrow, and changed the count copy to available titles. Local validation passes with 71 Catalog ChromeHeadless tests, the SSR/production build, and 375/768/1440px visual checks; no deployment was performed. |
| 2026-09-07 | Refined the public Catalog home calendar: renamed the navigation entry to **Les prochaines dates**, added a prominent next-books-fair card with the Website-inspired map/location treatment, and listed every upcoming Books event below it using the typed public schedule. Catalog validation passes 77 ChromeHeadless tests, the production build, and desktop/mobile browser checks; no deployment was performed. |
| 2026-09-07 | Corrected Catalog genre discovery: centralized five curated genre values, kept them available as a fallback in the home/search selectors, replaced the home genre pills with responsive cards linking to `/recherche?genre=...`, and added query/filter regressions. Catalog validation passes with 83 ChromeHeadless tests, the SSR/browser build, and 1280px/390px responsive checks; no deployment was performed and the local API was unavailable during the smoke. PR #84 is open. |
| 2026-09-06 | Refined the signed-out Catalog `/compte` experience into the V2 editorial layout with concrete member benefits, a compact access card, provider-neutral copy, and separate login/registration actions. Added the typed `prompt=create` registration redirect and ChromeHeadless coverage; 70 Catalog tests, the SSR/production build, Graphify refresh, and local `/compte` smoke pass. No deployment was performed. |
| 2026-09-06 | Added BackOffice **Comptes et rôles** administration: typed account models/facade, paged Entra account listing, account creation with temporary password, Tri/Caisse/Administrateur role assignment, API CQRS endpoints, Graph app-role synchronization, self-demotion guard, Bicep/Entra permission wiring, and regression coverage. Local validation passes with 5 Application, 1 Infrastructure, 13 API, and 17 BackOffice ChromeHeadless tests plus the BackOffice/API builds and Bicep compilation. No Entra consent, secret, Azure deployment, or real-account mutation was performed; `rtk` was unavailable so native Git commands were used. |
| 2026-09-06 | Fixed the Catalog external-reference watchlist action staying visually on `Ajout…` after a successful HTTP response: the zoneless search component now marks itself for check when the awaited add finishes, with a deferred-observable ChromeHeadless regression. Full Catalog tests and the production build pass; no deployment was performed. |
| 2026-09-06 | Fixed Catalog private-session handling: MSAL API-token renewal now preserves the current route and no longer becomes a generic account/admin error, while API access-token roles expose the administrator workspace in the account and navigation menus. Catalog validation passes 65 ChromeHeadless tests and the production SSR build. |
| 2026-09-06 | Refined the public Catalog UX: removed the on-site price from book details, showed the notification action only when no copy is available, clarified the in-catalog versus not-yet-in-catalog search sections, renamed the user list to the research list, hid the external provider label, clamped long reference metadata, and corrected the connected administration header label. Local Catalog validation passes 68 tests; PR #78 is open. |
| 2026-09-06 | Deployed `origin/main` commit `5601c2e` after merge CI runs `34049614770` and `34049623771`: infra `34050062897`, Books API/Worker `34050215862`, Website `34050215968`, BackOffice `34050216166`, Scan `34050216134`, and Catalog `34050216526` all succeeded. The final ACS configuration run `34050574089` verified domain ownership, SPF, DKIM and DKIM2 and confirmed Communication Service, Worker identity, Key Vault and Event Grid wiring. Public and internal smoke endpoints returned `200`; DMARC remains `NotStarted` and a real authorized-recipient email test is still pending. |
| 2026-09-06 | Resolved PR #67 on top of the current `main`: kept zoneless Catalog change-detection fixes and targeted post-scan metadata enrichment, removed the obsolete Blob-cover path after the direct-URL migration, and validated 58 Catalog tests, 319 backend tests, API/Worker/Catalog builds, and Graphify. No deployment was performed. |
| 2026-09-06 | Published the current `origin/main` commit `c490097` after PR #67: Books API/Worker deployment `34048120747` and Catalog deployment `34048120650` both passed; post-rollout public catalog/API smoke checks returned `200`. EF migrations were not rerun because DEV was already migrated. |
| 2026-09-06 | Deployed merged P2/P3 commit `ead9e79` to DEV (infra, Books API/Worker, Website, BackOffice, Catalog and Scan workflows green). Configured ACS email delivery with verified domain ownership/SPF/DKIM/DKIM2, linked Communication Service `vpd-acs-comm-dev`, Worker managed-identity permission, Key Vault webhook reference, enabled alert sending, and Event Grid delivery reports via workflow `34046166674`. DMARC remains `NotStarted` in ACS and no real test message was sent. Reproducible configuration workflow is in open PR #74. |
| 2026-09-06 | Completed the Catalog P2/P3 integration on top of the merged V2 visual refactor: typed external-reference search, edition/work follow actions, member alert preference, authenticated `/desinscription`, and all API-backed administration workspaces (overview, catalogue, sessions, dead stock, fairs, alerts, members and settings). Added 13 Catalog tests, 390px responsive checks and SSR smoke coverage; unsupported Entra role assignment and physical-carton concepts remain explicit limits. Local validation passes with 55 Catalog, 15 BackOffice, 82 Domain, 154 Application, 66 Infrastructure and 12 API tests. |
| 2026-09-06 | Migrated the Catalog Angular app to the reviewed V2 visual convention: shared real-logo navigation/dropdowns, four-column association footer, editorial home/search/detail/account/admin/legal/work layouts, responsive cards and mobile rules, generated fair calendar links, and design-system memory for all future Catalog work. Existing API contracts and truthful availability boundaries are preserved; unsupported admin modules remain explicitly unconnected. |
| 2026-09-06 | Implemented `feat/book-cover-direct-urls` in a dedicated worktree: book covers now use validated direct HTTPS URLs instead of Blob storage, with BnF validation, Open Library fallback, exact-ISBN Google Books fallback, `CoverSource`/`CoverCheckedAt`, a migration that clears legacy blob references, and a shared non-striped placeholder for Catalog and Scan. The optional Google Books key is wired through Key Vault. Azure deployment and the PR remain pending. |
| 2026-09-05 | Merged PR #65 (`f3fd148`) to close the overnight review. The final main CI `33934370102` passed backend tests, MAUI Android, all Angular builds, and API/Worker/Catalog container builds. The review found no blocking defect; it recorded the exact-route robots maintenance guard and kept ACS, Worker heartbeat, benchmark, offline and physical gates explicitly open. No PR remains open. |
| 2026-09-06 | Implemented the P2/P3 books administration/API tranche: typed CQRS reads and writes for catalogue, stock, fairs, sessions, alerts, members and settings; external bibliographic search; append-only session corrections; persisted optional fair revenue; member alert preferences; responsive BackOffice `/administration`; and a complete `src/Catalog` refactor handoff. `src/Catalog` itself remains intentionally untouched. Backend and BackOffice validation are recorded on the delivery branch; migration application, ACS and real e-mail remain external checks. |
| 2026-09-05 | PR #64 (`d097792`) merged the final overnight plan/memory update. The main CI run `33933202774` passed backend tests, MAUI Android, all Angular builds, and the API/Worker/Catalog container builds. Final review found no blocking defect; the exact-route robots policy is documented as a maintenance point for any future private child route, and ACS/heartbeat/benchmark/physical gates remain open. |
| 2026-09-05 | Merged PR #63 (`3a6e887`) after correcting the Catalog SSR robots contract: the static shell defaults to `noindex, nofollow`, and navigation updates the meta directive for public versus private routes. Catalog CI checks `33931556397` and `33931558967` passed; `Catalog - deploy` `33932087193` rolled `vpd-catalog:3a6e887`, and live smoke confirmed public `index, follow`, private HTML/header `noindex, nofollow`, and healthy robots/sitemap endpoints. |
| 2026-09-05 | Merged PR #61 (`dcc0c23`) after fixing the SQLite/Aspire account-deletion lookup and the missing member-data cleanup during local deletion/anonymization. Watchlists, alert history, bounce events, and `AlertEmail` outbox rows are removed transactionally while retained book movements and scan sessions remain auditable. The backend suite passes 291 tests; CI checks `33929259723` and `33929261525` are green, and `Books runtime - deploy` `33929828651` rolled API/Worker tag `dcc0c23` without migrations. Public API/catalog/Scan, DNS, and private-route smoke checks are green. |
| 2026-09-04 | Fixed the Scan API 401 regression: MSAL Angular 5 strict matching did not consider `/scan` a prefix, so the interceptor skipped the bearer header for `/scan/catalog/delta` and `/scan/sessions`. Protected the nested endpoints with `/scan/*`, added a ChromeHeadless regression, and recorded that the Scan deployment must be rerun. |
| 2026-09-05 | Fusionné la PR #57 (`64c347e`) et redéployé la Scanette par `Scan - deploy` `33924618301` : le wildcard MSAL `/scan/*` ajoute désormais le bearer token aux routes imbriquées `/scan/catalog/delta` et `/scan/sessions`. Le smoke public catalogue/Scan/API est vert ; le retest interactif avec un compte `Tri` reste à faire. |
| 2026-09-05 | Corrigé puis fusionné via la PR #58 (`6a5a736`) le regroupement des notices BnF sans `WorkId` : Open Library enrichit uniquement cet identifiant, et une panne Open Library conserve la notice BnF. Le runtime API/Worker `33924236821` a été redéployé sans migration ; 286 tests backend locaux passent et le smoke metadata public renvoie une notice BnF avec `WorkId=OL10263W`. |
| 2026-09-04 | Ran `Books runtime - deploy` `33908408641` from `main` commit `585a0ac` with `run_migrations=true`: API and Worker were built with the shared tag `585a0ac`, all pending EF migrations were applied to DEV Azure SQL, the temporary SQL firewall rule was removed, and both Container Apps rolled out successfully. API/catalog and public catalog/Scan smoke checks return `200`; the new `Sweep`/`Enrich` heartbeat and P1-9/P1-11 measurements remain to be verified. |
| 2026-09-04 | Added the P1-9 dead-stock administration read: TDD `GetDeadStockQuery` over the movement ledger, the `GET /books/admin/dead-stock` Administration endpoint, typed contracts, an `Isbn13 + Type + OccurredAt` SQL index migration, and API/model authorization regressions. The local query and integration validations pass; the benchmark and Azure rollout remain pending. |
| 2026-09-04 | Incorporated Scanette field feedback locally: cash and consultation now open the live camera immediately and restart it after each decoded book, cash entries can be removed individually, the verdict card is compacted, and `RG-04` flags a repeated ISBN within five seconds. Session termination now flushes and closes the remote session before clearing the active local snapshot, preventing a completed session from being resumed. Validation passes with 74 ChromeHeadless tests, the bootstrap contract, the production Scan build, and `graphify update .`; no deployment was performed. PR #52 is open for review. |
| 2026-09-04 | Corrected the Books catalog/Scan handoff locally: the next-fair projection now uses the fair calendar date while treating legacy opening/closing hours as wall-clock components, and Scan now auto-replays decided gestures, requires a final verdict before ending, and durably retries session closure. Full backend validation passes (273 tests) plus 78 ChromeHeadless tests and the production Scan build. After the Books runtime/migrations rollout, live read-only Azure checks still show zero public books and the stale March fair because this correction has not been deployed; a browser replay or rescan remains the operational follow-up. |
| 2026-09-04 | Corrected the Scan role gate: after Entra showed the current account assigned both `Administrateur` and `Benevole trieur` on `vpd-api-dev`, the client was found to be reading roles from the cached ID token instead of the API access token. Scan now acquires the API scope silently and reads the access-token `roles` claim, with a regression covering multiple roles; 62 ChromeHeadless tests and the production build pass. The Scan deployment remains pending. |
| 2026-09-04 | Completed the post-merge public rollout from `main` commit `9f8ec55`: `Catalog - deploy` `33906639354`, `Scan - deploy` `33906624368`, and the retried `Infra - deploy` `33906654599` succeeded. OVH/Azure HTTPS smoke tests return `200` for the catalog and Scan; Entra portal verification shows the canonical SPA URIs on `vpd-catalog-dev` and `vpd-scan-dev`. A real Scan login redirect reaches the role gate and correctly rejects the current account without `Tri`. The explicit API/Worker runtime rollout and EF migrations remain pending because migration 0 intentionally deletes legacy `Users`. |
| 2026-09-04 | Validated OVH DNS and Azure Container Apps managed HTTPS for `livres.volepapillondamour.fr` and `scan.volepapillondamour.fr`; deployed the catalog API as `vpd-api:dc4ec74` with public catalog routes returning `200`; aligned dev Bicep SNI bindings, the Scan production redirect origin, Docker build defaults, deployment workflow, Entra documentation, and NEXT state. The actual Entra URI update and Scan rollout remain pending manual MFA approval. |
| 2026-09-04 | Implemented the P2 public books catalog from the existing main branch: anonymous catalog/work/fair/sitemap API reads with typed projections and regression tests; a separate Angular 21 SSR `src/Catalog` app matching the catalog mockups; Docker/AppHost/CI integration; manual catalog deployment workflow; and deployment-ready ACA infrastructure with the custom domain intentionally unbound. No Azure or DNS change was made. |
| 2026-09-04 | Corrected legacy event date/time handling across BackOffice and Website: API UTC wall-clock values are normalized before Angular Material pickers, event date displays are explicit UTC, and Books detail uses `HourOpenDoors` for the start time. Added regression coverage; no API/schema migration or deployment. |
| 2026-09-04 | Corrected the Website home "Prochains rendez-vous" cards to use the detail-page time rule: Books events use `hourOpenDoors`, while Bingo and Other events use `dateStart`. Added three-type regression coverage; 67 Website tests and the production build pass. No API/schema or deployment change. |
| 2026-09-04 | Fixed the Scan login no-op from `main`: tenant-scoped the CIAM authority in development and production, added an explicit `redirectStartPage`, surfaced MSAL redirect-start failures in the login screen, and added authority/login regressions. The dedicated worktree was verified with 61 ChromeHeadless tests and the bootstrap contract; no deployment was performed. |
| 2026-09-04 | Created `feat/scanette-auth-camera` from `main` in a dedicated worktree. The Scan PWA now gates access on the Entra `Tri` role, returns to its login surface when the session/renewal fails, initializes MSAL before refresh, removes the top toast stack, starts the live camera automatically in tri mode, and keeps the phone shell centered without page overflow. Added 60-test/bootstrap/build validation and recorded the camera-permission browser limitation; no deployment was performed. |
| 2026-09-04 | Completed the local P1-6/P1-7/P1-8 runtime foundation: added the scheduled Books `Sweep` and `Enrich` Functions, retryable metadata/cover enrichment, ACS email content/sender kept disabled by default, exact-lease alert delivery, cancelled-fair lifecycle, App Insights caps/alerts, `book-covers` storage, restricted CORS, an explicit pre-rollout migration policy, and the manual `Books runtime - deploy` workflow. Added the account-deletion outbox kind-isolation regression. Local validation is being rerun before the dev deployment; Azure domain verification and physical scan gates remain external. |
| 2026-09-04 | Integrated the Scanette mockup visual locally across home/session mode, tri verdicts, offline/manual flows, session summary, cash, and consultation. Consultation reads the local catalog without an outbox gesture; cash remains a local visual list pending durable sale wiring. Validation passes with 53 ChromeHeadless tests, the production build, and 390px/1280px browser checks. No deployment was performed. |
| 2026-09-04 | Corrected the metadata probe's external-dependency failure contract: a simultaneous BnF/Open Library outage is now exposed as `503 Service Unavailable` instead of `500`, while the resolver exception remains available for Worker retry semantics. Added an API middleware regression test and kept the correction local; no Azure deployment was performed. |
| 2026-09-04 | Completed and deployed the P1-6/P1-8 runtime slice: PR #39 merged as `abbb336`, both complete CI runs passed after aligning MAUI CI to `net10.0-android`, infra what-if `33822673986` and DEV deployment `33822751659` succeeded, and runtime `33822924593` built the API/Worker shared tag `abbb336`, applied all seven pending EF migrations before rollout, and closed the temporary SQL firewall rule. `/health` returns `200 Healthy`; the first metadata smoke returned `500` during a temporary provider outage and was later retested successfully after Open Library recovered. ACS remains disabled while domain verification is underway; new Worker heartbeats, alert delivery, P1-9 SQL measurement, and physical P1-10/P1-11 gates remain open. |
| 2026-09-03 | Completed the local P1-5 Scan slice: typed `Tri`-protected catalog delta/session/scan/close contracts, hidden-entry removal and watchlist-state reprojection, IndexedDB `catalog`/`outbox`/`session` persistence, local verdicts, durable offline gestures, MSAL authentication, PWA service worker, and sequential idempotent outbox replay. Added migration `20260903211547_AddWatchlistUpdatedAt` with a UTC watchlist watermark backfilled from `CreatedAt`. Validation passes with 72 Domain, 93 Application, 29 Infrastructure, and 49 ChromeHeadless tests, the backend solution build, no pending EF model changes, and Scan production/development builds. No Azure or production migration was performed; QT-02/QT-03/QT-08 and worker delivery remain external. |
| 2026-09-03 | Added the ACS/Event Grid delivery-report transport for `RG-31`: typed Event Grid/ACS contracts, synchronous subscription validation, shared-secret authentication through `X-Vpd-EventGrid-Secret`, recipient-to-member resolution, and safe acknowledgement of delivered/expanded reports, unknown recipients, and members without a watchlist. The endpoint is local only; Azure Event Grid delivery properties, the webhook secret, worker sending, and the remaining Books API remain pending. Validation passes with 72 Domain, 88 Application, and 29 Infrastructure tests plus the full solution build/test. |
| 2026-09-03 | Completed the P1-4 provider-event idempotence slice for `RG-31`: `RecordEmailBounce` now validates and records the ACS/Event Grid provider identity in `EmailBounceEvents`, uses a unique provider-event index, returns the current state on a sequential replay without incrementing `BounceCount` again, and rejects an identity already owned by another member. Added migration `20260903192839_AddEmailBounceEventLedger`; validation passes with 72 Domain, 84 Application, and 29 Infrastructure tests, full solution build/test, and no pending EF model changes. The ACS/Event Grid adapter, API exposure, worker sending, and Azure migration remain pending. |
| 2026-09-03 | Added the P1-4 `RG-31` bounce-handling primitive with TDD domain rules and a transactional `RecordEmailBounce` handler. `Watchlist` suspends alerts after three consecutive bounces and resets the consecutive count after a successful delivery without automatically reactivating a suspended list. Validation passes with 70 Domain, 82 Application, and 29 Infrastructure tests; provider event idempotence, ACS/Event Grid transport, worker sending, Azure migration, and API exposure remain pending. |
| 2026-09-03 | Added the P1-4 `RG-45` alert administration slice with TDD `CancelBookAlerts` and `ForceBookAlerts` handlers. Pending alert rows can be cancelled or made immediately due without changing stock; `ReassignSessionMode` now cancels and rebuilds pending alerts in the same transaction and never recreates sent alerts. Validation passes with 67 Domain, 80 Application, and 29 Infrastructure tests. The post-window QT-02 check was attempted in Azure but both the worker and `vpd-law-dev` returned 401 because the active token tenant differs from the subscription tenant; no Azure change was made. Bounce handling, transport, worker sending, and Azure migration remain pending. |
| 2026-09-03 | Added the P1-4 watchlist and alert-queue slice with TDD `Watchlist`/`WatchlistItem`/`UserAlertHistory` domain types, the transactional `BookAlertOutbox`, edition/work matching, member grouping, configurable delay, cooldown and suspended-member filtering, plus no-alert handling for undated fairs. Added migration `20260903185500_AddBookWatchlistsAndAlerts`; validation passes with 67 Domain, 77 Application, and 27 Infrastructure tests, full solution test/build, and no pending EF model changes. Worker sending, alert cancellation/force, bounce handling, API/transport, Azure migration, and QT-02 remain pending. |
| 2026-09-03 | Completed the next P1-4 internal book slice with TDD handlers for manual metadata correction and safe book deletion. Selected metadata fields are merged and permanently locked against automatic refresh; deletion is refused when any sale, movement, or announcement history exists. Validation passes with 61 Domain, 76 Application, and 21 Infrastructure tests, the full solution test/build, and no pending EF model changes. Alert outbox, API/transport, Azure migration, and QT-02 remain pending. |
| 2026-09-03 | Extended P1-4 locally with TDD handlers for `RegisterSale`, `VoidSale`, `AdjustQuantity`, association settings, undated-announcement attachment, rare/catalog flags, and `ReassignSessionMode`. Sales resolve a unique open Books fair, support zero-stock warnings and idempotent replay; voids are limited to the open fair and create a traced inverse. Session reassignment performs inverse/replay movements and is idempotent. Added migration `20260903181307_AddSaleReversalLink`. Validation passes with 57 Domain, 70 Application, 21 Infrastructure tests, a clean solution build, and no pending EF model changes. Metadata correction/deletion, alert outbox, API/PWA, Azure migration, and QT-02 remain pending. |
| 2026-09-03 | Started P1-4 locally with TDD application handlers for `ScanBook`, `OpenScanSession`, and idempotent `CloseScanSession`: ISBN normalization and canonical redirects, RG-15 verdict calculation without external calls, atomic book/session/movement/announcement writes, clock-suspect timestamps, active-session uniqueness, and gesture replay. Added `ClientGestureId` to `BookAnnouncements` with migration `20260903175445_AddClientGestureIdToBookAnnouncements`. Validation passes with 57 Domain, 47 Application, 21 Infrastructure tests and a clean solution build; API exposure, alert outbox, remaining handlers, Azure migration, and QT-02 remain pending. |
| 2026-09-03 | Implemented the P1-3 books foundation locally: added the Book, BookMovement, ScanSession, AssociationSettings, and BookAnnouncement domain types, five EF Core configurations, UTC DateTime converters, model metadata tests, and migration `20260903173750_AddBookExchangeCore`. Domain (49), Infrastructure (21), and solution test runs pass; Azure SQL has not been migrated and QT-02 remains open. |
| 2026-09-03 | Recorded the user-reported `S0-4` campaign as successful across 300 real books without fabricating unprovided sub-metrics. Deployed `P1-1` through `Infra - deploy` run `33780715179` with the Functions timer at `minReplicas: 0`/`maxReplicas: 1`; the two-hour observation remains open. |
| 2026-09-03 | Completed the documentation-only `P1-2` design pass for the books exchange: `DT-17` fixes UTC persistence with `Europe/Paris` calendar interpretation, `DT-18` defines Pending/final local outbox states and cancellation semantics, `DT-19` selects ISBN redirection for merges, `DT-20` defines the open-fair interval and overlap behavior, and `DT-21` selects Jasmine/Karma/ChromeHeadless with real IndexedDB and simulated transport. P1-3 implementation remains gated by `QT-02`. |
| 2026-09-03 | Hardened the Scan result card when a bibliographic cover URL is unavailable: it retries an ISBN-based Open Library cover and then renders an explicit unavailable-cover placeholder. Added regression coverage; 24 ChromeHeadless tests and production/development builds pass. |
| 2026-09-03 | Corrected the Scan probe's zoneless Angular refresh gap: manual ISBN results, camera detections, and photo errors now render immediately after asynchronous work instead of waiting for another click. Photo decoding also retries cropped, resized, and thresholded canvas variants; 22 ChromeHeadless tests plus production/development builds pass. A supplied photo of a moiré-covered screen still cannot be considered a reliable barcode test. |
| 2026-09-03 | Corrected iPhone ISBN camera detection in the Scan probe after the deployed camera preview activated but did not recognize 1D barcodes. Replaced `html5-qrcode` with `@zxing/browser`, enabled ZXing `TRY_HARDER`, scans the full video frame, supports EAN-13/EAN-8 and QR formats, and keeps photo fallback. Fifteen ChromeHeadless tests pass and the production build succeeds with only its initial bundle-budget warning; deployment and manual iPhone retest remain pending. |
| 2026-09-03 | Deployed the merged Scan/Worker delivery from `main` commit `728939f`: `Infra - deploy` `33758954044`, `Scan - deploy` `33759976040`, and `Worker - deploy` `33759966121` all succeeded. The public Scan HTTPS FQDN and API metadata endpoint return `200`; the native Functions-on-Container-Apps worker is healthy, discovers `AccountDeletionSweepFunction`, and completed its first scheduled sweep successfully with zero pending deletions. |
| 2026-09-03 | Added the iPhone-capable Scan delivery path: `html5-qrcode`/ZXing camera decoding with photo fallback, a production-shaped nginx image, public HTTPS ACA infrastructure, native Functions-on-Container-Apps worker infrastructure, dedicated App Insights/managed identities, and manual OIDC deployment workflows for Scan and Worker. The GitHub `development` environment now contains the Entra Graph client credentials required by the worker. |
| 2026-09-03 | Corrected local Aspire startup after the S0-2 scan probe: populated the development Entra API client/audience to prevent `IDW10106` on the anonymous metadata endpoint, isolated the Functions worker from API authentication and MediatR/Mapster registration, and stopped overriding Aspire's dynamic Azure Functions storage endpoint. Aspire smoke validation returns `200 OK` for `GET /books/9783140464079/metadata`, the worker acquires its host lock, 94 backend tests pass, and the backend solution builds with no errors. |
| 2026-09-03 | Implemented the local S0-2 Angular scan probe: typed ISBN-10/13 normalization, keyboard-wedge and native camera capture, the anonymous `/books/{isbn13}/metadata` endpoint, BnF SRU → Open Library resolution, AppHost LAN wiring on port 4202, and CI build coverage; 92 backend and 9 Scan tests plus production/development builds pass. |
| 2026-09-03 | Migrated `MauiCashApp` to `net10.0-android` and MSAL.NET 4.88.0: added silent-first Entra token acquisition, the Android `msal...://auth` callback, a Refit bearer handler, and a focused unit test; Android compilation remains blocked locally by the missing SDK (`XA5300`). |
| 2026-09-03 | Migrated the BackOffice from the cookie/JWT login to MSAL Angular 5.3.1 with MSAL Browser 5.20.0: `MsalGuard` protects routes, `MsalRedirectComponent` handles redirect responses, and an Axios adapter acquires the Entra API scope silently. Removed the legacy auth services/dependencies and recorded the 5-test/build validation. |
| 2026-09-02 | Prepared the Entra Graph setup for execution on an authorized workstation: added the missing `Microsoft.Graph.Identity.SignIns` prerequisite and module guards, documented the real tenant/redirect URIs and target administrator, and recorded the local Conditional Access limitation. |
| 2026-09-02 | Actualisé la section `/maxence/vie-quotidienne/soins-quotidiens` sur l’alimentation parentérale : durées, poches sur mesure, aménagement de la chambre et protocole stérile, avec repères visuels et test de contenu |
| 2026-09-02 | Enrichi la fiche Website `/maxence/maladies/hirschsprung` avec le nouveau texte explicatif, la forme étagée de Maxence et une mise en page éditoriale responsive, avec un test de contenu dédié |
| 2026-09-02 | Started L0-11 deployment 1 on `feat/l0-11-api-entra`: added `Microsoft.Identity.Web` 4.14.2, staged Entra/legacy JWT scheme selection, and the `Tri`/`Caisse`/`Administration` policies. The legacy login path and JWT settings remain intentionally; backend restore, 71 tests, solution build, and Bicep compilation pass, with no Entra registration or Azure deployment performed. |
| 2026-09-02 | Started L0-11 with an idempotent, dry-run-safe Entra application setup: redirect URIs are merged, the Android MSAL redirect is derived for the cash app, and new applications are represented during `-WhatIf`; execution remains pending PowerShell 7 and Microsoft Graph modules. |
| 2026-09-02 | Applied the L0-10 platform decision: `MauiCashApp` now targets Android only (`net9.0-android`); durable APK signing and organized redistribution remain deferred because no keystore exists yet. |
| 2026-09-02 | Configured the dev SQL parameter for the decision's fixed Azure SQL `S1` tier (Standard, 20 DTUs, 250 GB, no automatic pause), aligned the `SqlServer` SKU type descriptions, and verified both Bicep files compile; no Azure deployment was performed. |
| 2026-09-02 | Added the API `/health` endpoint with an Infrastructure database connectivity check, configured API readiness/liveness/startup probe parameters on `/health:8080`, and verified the endpoint locally through Aspire plus Bicep compilation. |
| 2026-09-02 | Added `.github/workflows/ci.yml` for push and pull-request validation of the backend solution/tests, Android MAUI target, and both Angular builds; the first GitHub run remains pending because workflows were not launched in this session. |
| 2026-09-02 | Made SharedUi dependency linking application-local: moved the linker to `src/SharedUi/scripts`, invoked it from both Angular apps' build/start hooks, and verified BackOffice builds after its own `npm ci` without Website installation |
| 2026-09-02 | Retired the former code-graph integration from the repository guidance and local developer tooling, and removed its local indexes and installation. |
| 2026-09-02 | Enabled Graphify as the repository-wide code/corpus graph: added the workspace MCP server, platform hooks and skills, `.graphifyignore`, local bootstrap/refresh commands, and aligned agent guidance and memory. |
| 2026-09-02 | Added independent product website visibility: BackOffice toggle, persisted `VisibleOnWebsite` flag, public `/product/public` projection for Website, and regression tests while keeping the full `/product` feed for cash |
| 2026-09-02 | Corrected the GA4 `dataLayer` queue to use the standard `arguments` shape and initialized denied Consent Mode defaults before events are sent. |
| 2026-09-01 | Ajouté GA4 au consentement d'audience du Website avec suivi des routes et révocation, remplacé le sitemap placeholder par les URLs publiques réelles et ajouté `robots.txt` ainsi que la documentation Search Console |
| 2026-09-01 | Remplacé les spinners et placeholders d'attente du Website par des skeletons qui conservent les gabarits réels : trois cartes sur l'accueil, agenda, actualités, détails, lots et tableau live, avec états d'erreur finis et animation respectueuse de `prefers-reduced-motion` |
| 2026-09-01 | Complété `/nos-actions` avec les cinq actions illustrées (équithérapie, ordinateurs, deux actions autour des jouets et permis de conduire), supprimé les quatre cartes statistiques et reformulé l'introduction autour de l'aide et de la générosité |
| 2026-09-01 | Remplacé les placeholders de la mosaïque d'actions de la page d'accueil par les trois photos fournies (équithérapie, ordinateurs avec le Kiwanis, collecte de jouets), ajouté le recadrage responsive et reformulé la section autour des aides concrètes rendues possibles |
| 2026-08-24 | Rendu la revue de presse atteignable depuis le header : `SiteNavItem` accepte desormais `children`/`hint`, l'entree "L'association" ouvre un sous-menu CSS (group-hover + group-focus-within, `invisible` pour garder l'ordre de tabulation propre) listant presentation/comment-aider/revue-de-presses/photos, et le menu mobile deplie les memes sous-rubriques avec une liste scrollable ; la page revue de presse reste un placeholder sans article |
| 2026-05-20 | Completed a focused backend live loto tableau business-core pass for add numero, undo/back, and won-lot validation: added validator/domain/handler tests, hardened multi-party rollback and bingo undo, and verified 100% line/branch on targeted live business methods |
| 2026-05-19 | Hardened the live loto tableau flow with event-scoped SSE broadcasts, safer rollback/win handlers, Website SSE parse/reconnect guards, new Application/Infrastructure test projects, and verified 100% targeted coverage on live handlers/SSE manager plus backend and Website validations |
| 2026-05-19 | Dream consolidation: verified the May 18 durable stack/runtime/frontend facts were already present in thematic memory, made no thematic rewrites, and reset the dream gate |
| 2026-05-18 | Migrated the Website Sass entrypoint from deprecated `@import` to `@use`/`@forward`, verified the Website build stays green, and removed the `angular-sass` deprecation warnings while leaving unrelated bundle-budget/CommonJS warnings untouched |
| 2026-05-18 | Fixed Aspire SQL Server local auth drift by making the AppHost SQL password explicit and secret-backed (`Parameters:sql-server-password`), enabling AppHost user secrets, validating that the SQL container restarts without `18456` login failures, and preserving the existing data volume |
| 2026-05-18 | Fixed Rider/Aspire launch failures caused by duplicate Domain assembly attributes by excluding `src/Backend/Vole_Papillon_Damour.Domain/artifacts/**` from the Domain SDK default items, then verified Domain build, AppHost build, and AppHost startup all succeed again |
| 2026-05-18 | Fixed Aspire AppHost frontend startup by removing the extra leading `--` from both `AddNpmApp(..., args)` calls, verified `npm run start -- --host ... --port ...` works for BackOffice and Website, and noted a separate baseline AppHost build blocker from duplicate Domain assembly attributes |
| 2026-05-18 | Created an Infra Flow Sculptor project for ACA deployment of the API, BackOffice, and Website only, added `infra/aca/` Bicep + build helper + frontend Dockerfiles, separated shared/common vs application infra, and validated the ACA Bicep plus both frontend Docker images |
| 2026-05-18 | Introduced shared `@vpd/ui` design system under `src/SharedUi/` (TS path-mapped, no library project), mutualized title/under-section/button/image/actuality-card/event-card/product-card/product-list/pipes across BackOffice and Website with a display-vs-`[editable]` pattern, refactored both apps' wrappers and SharedModules, deleted per-app duplicates, fixed `NumberLineEnum` to string values, widened `DsVpdEventModel.eventType`, and verified `ng build` passes on both apps |
| 2026-05-18 | Removed the backend OCR bingo-card slice and the matching BackOffice admin scan UI, cleaned AppHost/API OCR configuration, and verified that Website had no OCR surface while BackOffice build issues remain baseline and unrelated |
| 2026-05-18 | Added a Website legal slice with prerendered legal routes, footer-driven legal navigation, and content placeholders for unresolved association identifiers, publication director, retention windows, hosting details, and accessibility remediation facts |
| 2026-05-18 | Dream consolidation: corrected lingering Angular 18 references, aligned runtime memory with the inactive MailingList API wiring, refreshed the code graph snapshot, and reset the dream gate |
| 2026-05-12 | Bootstrapped and expanded the repository memory around the actual .NET 10 backend, Angular 21 + Website SSR frontends, Aspire AppHost orchestration, auth/build/endpoints/design-system guidance, removed active mailing-list and actuality-email broadcast flows from the documented runtime, and captured the Website shell and SSR hardening work |
 | 2026-09-03 | Fixed the BackOffice blank-page refresh race by awaiting `MsalService.initialize()` through Angular's application initializer before cached-account services and MSAL redirect handling start. |
 | 2026-09-03 | Fixed Entra app-role authorization for BackOffice writes: inbound claim mapping is disabled for the Entra bearer scheme so the token's `roles` claim satisfies the `Administration` policy. |
| 2026-09-03 | Corrected the Entra v2 API audience configuration for BackOffice writes: API validation now receives the application ID carried by the token, while the delegated `api://.../access_as_user` scope remains unchanged. |
| 2026-09-03 | Consolidated the current delivery state: refreshed Website editorial content and association imagery; harmonized the four Maxence daily-life detail pages with a shared chapter header, return navigation, and wider reading column; staged Entra/MSAL identity across API, BackOffice, and MAUI with account deletion; delivered and deployed the Scan ISBN probe plus the private Worker; fixed BackOffice bootstrap, Scan async rendering, and cover fallback. Local validation results are recorded, including the remaining MAUI/CI target drift (`net10.0-android` in the project versus `net9.0-android` in the workflow). |
| 2026-09-02 | Advanced the platform foundation with Entra/Graph setup, health probes, the Azure SQL S1 parameter, SharedUi linking, Graphify, CI build coverage, public product visibility, GA4/SEO, event and design refinements, and the bourse aux livres technical documentation. |
| 2026-09-01 | Added concrete association actions and supplied photos, skeleton loading states, production-host configuration, and the first complete bourse aux livres functional/technical specifications. |
| 2026-09-07 | Merged the Catalog genre changes in current `origin/main` (`60b0f907`, PR #84): five curated fallback genres, responsive cards, and `/recherche?genre=...` query/filter behavior; the same-day hero and upcoming-fairs refinements are also recorded. Catalog validation reaches 83 ChromeHeadless tests, SSR/production build, and 1280px/390px checks; the local API was unavailable during smoke. Added the dry-run-safe External ID signup-flow script with Pester coverage; no tenant mutation was performed. The current worktree additionally adds Scan observability (Books traces/metrics, browser correlation, service naming and a slow-metadata alert), with backend/Scan/Bicep validation passing and Azure deployment/live verification pending. |
| 2026-09-06 | Consolidated Catalog and identity UX: V2 member/admin surfaces, external-reference watchlist behavior, private-session renewal, and BackOffice account/role administration are implemented with typed contracts and local regression coverage. DEV `origin/main` `5601c2e` deployment runs for infra/API-Worker/Website/BackOffice/Scan/Catalog and ACS succeeded; domain/SPF/DKIM/DKIM2 and identity/Key Vault/Event Grid wiring are verified, public smoke is `200`, direct HTTPS covers are deployed, DEV migrations were current, and DMARC/authorized-recipient email remain open. |
| 2026-09-05 | Merged the overnight review/runtime PRs (#65, #64, #63, #61), the Scan nested-route bearer fix (`/scan/*`), and the BnF/Open Library `WorkId` grouping fix. CI/deploy and public API/Catalog/Scan/DNS smoke were green; robots are route-aware, account deletion retains auditable book history while removing member-only data, and Worker heartbeat, ACS, benchmark, offline, physical and interactive `Tri` checks remain open. |
| 2026-09-04 | Completed the Books/Scan DEV and public-surface rollout: explicit EF migration/runtime workflows, P1-9 dead-stock administration, P1-5 Scan offline/auth/camera/session behavior, public Catalog/Scan domains, and SSR robots rules are recorded. API health and public smoke were `200`; Worker heartbeat, ACS verification, benchmark, offline and physical gates remain open. |
| 2026-09-03 | Consolidated verified P1-3/P1-4/P1-5 Books work: typed books, movements, sessions, announcements, sales, metadata correction/deletion, watchlists and alert outbox/administration; ACS/Event Grid bounce/delivery-report idempotence; and Scan IndexedDB persistence with sequential gesture replay. The user-reported S0-4 campaign covered 300 real books without invented sub-metrics; Scan/Worker smoke was green, while Azure migration, Worker delivery, Android SDK/physical retests and QT-02/QT-03/QT-08 remained external gates at the time. |
| 2026-09-02 | Consolidated platform foundation work: staged composite Entra/legacy auth and `Tri`/`Caisse`/`Administration` policies; prepared Graph app setup; fixed the Android target to `net10.0-android`; added `/health`, CI build coverage, fixed local SharedUi linking, set SQL `S1`, and enabled the repository Graphify guidance after retiring the former integration. Website/product visibility, GA4 consent, SEO and editorial updates were also recorded. |
| 2026-09-01 | Consolidated Website presentation work: consent-gated GA4 with denied defaults and revocation, real robots/sitemap/Search Console configuration, skeleton loading with reduced-motion support, and the illustrated association actions/photos with responsive crops. |
| 2026-08-31 | Clarified the bourse aux livres scan workflow, pre-scan availability modes, deferred valuation, and delayed post-session alerts. |
| 2026-08-25 | Updated Website legal information and contact details for the compliance pages. |
| 2026-08-24 | Improved Website navigation, presentation, event locations, viewport/accessibility behavior, responsive layouts, and production data snapshots; the association review-of-press page remains a placeholder. |
| 2026-08-23 | Hardened deployed Website SSR proxy/host handling, frontend styles and consent shell, container deployment defaults, and repository cleanup. |
| 2026-09-07 | Prepared Scan Lot 1 hardening on `fix/audit-scan-offline-operability`: cached Entra authorization now exposes a degraded local mode, close requests survive while a new local session opens, old pending gestures are isolated as `Orphaned`, outbox replay continues past failures with transient backoff and five-attempt 4xx quarantine, and cash/consultation report local-catalog failures without creating a false non-rare sale line. Scan tests and build pass locally; physical offline/device retest remains required. |
| 2026-09-08 | Prepared Scan Lot 2 hardening on `fix/audit-scan-cash-safety`: scanner login is subscribed, IndexedDB connections reopen after version changes or blocked opens, storage/closure failures are explicit, cash/session destructive actions require confirmation, pending validated sales can be cancelled, and the manual ISBN keypad accepts `X` with an accurate 10-or-13-character counter. |
| 2026-09-08 | Prepared Scan Lot 3 on `fix/audit-scan-quality-debt`: separated local decision/transmission counters and surfaced synchronization across triage/cash/consultation, added the next fair schedule and synchronized correction window, made photo decoding lazy, aligned admin verdict defaults, neutralized camera guidance, and removed the requested dead code. Scan 122 tests, Catalog 104 tests, backend solution 329 tests, and both frontend builds pass; the root `dotnet test` remains structurally invalid because no solution is present at repository root. |
| 2026-09-08 | Implemented audit catalogue/Scanette lot 1 on `fix/catalogue-scan-lot1`: API-01/02/03/07, SCAN-03 and CAT-01/02 with regression tests, without a database migration; local backend, Scan and Catalog validations pass, while physical offline, RGPD and watermark-contract decisions remain open. |

| 2026-09-08 | Prepared Scan Lot 4 on `fix/audit-scan-accessibility`: labeled Scan containers now expose explicit roles, the live manual ISBN value is announced without an overriding label, and every operating screen keeps a valid `h1` landmark target in both empty and verdict states. S-22 remains pending a real USB scanner validation. |

| 2026-09-08 | Prepared Catalog Lot 5 on `fix/audit-catalog-public`: home cards, hero and navigation now consume only API genres, the public count is filtered to available titles, Maps is a static external link without an embedded third-party frame, the footer year is dynamic, and the personal Facebook profile is removed. Existing direct publisher/host legal details were verified rather than rewritten. |
| 2026-09-11 | Prepared Scan synchronization UX on `fix/scan-auto-sync-header`: removed the global return/synchronization chrome, kept automatic sync on arrival/reconnect/retry, added a transient success toast, and replaced expanded alerts with a compact offline/action strip plus an actionable status modal; 170 Scan ChromeHeadless tests and the production build pass locally. PR [#127](https://github.com/FlorianDrevet/Vole-Papillon-Damour/pull/127) is open; connected production smoke and deployment remain pending. |
| 2026-09-11 | Reworked Catalog `/administration` to the AdminSidebar/maquette shell across all admin screens, added typed Entra volunteer account operations, kept inventory physical gaps explicit, and validated 130 Catalog tests, production build, Graphify and local browser shell; no deployment. |
| 2026-09-11 | Refactored the authenticated Catalog account page around a two-tab watchlist/preferences surface matching the member mockup, added typed availability/cover/alert card details, and changed the header account trigger to an outlined anonymous state versus a filled full-name connected state. Catalog tests, build, Graphify and desktop/mobile 390×844 smoke pass; the authenticated connected state was not manually exercised without credentials and deployment remains pending. |
| 2026-09-11 | Corrected the Catalog administration follow-up: dashboard titles are left-aligned, period chips reload 30-day/3-month/year overview ranges, the scan-session badge counts only correctable sessions, and session projections use first/last names without an email fallback. Made the fair/statistics queries provider-safe, mapped Entra Graph transport/payload failures to 503, tolerated omitted Graph collections, repaired the Settings icon, and restyled the public sort select. Domain/Application/Infrastructure/API `86/183/74/16` tests, 147 Catalog tests, both builds, and 1280/390px Chrome checks pass; authenticated data smoke, deployment, Graph runtime configuration and Books migration verification remain pending. |
| 2026-09-11 | Moved Catalog external-title following above the reference list with an explanation of all-editions versus precise-edition tracking, removed duplicate work-follow buttons from edition cards, hid the work action for empty results, and added regressions for multiple editions and empty searches. |
