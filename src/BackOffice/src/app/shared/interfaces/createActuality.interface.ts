export interface CreateActualityInterface {
  id: string,
  title: string,
  PrincipalImage: string,
  images: string[],
  article: string,
  date: Date,
  facebookLink: string | null,
  instagramLink: string | null,
}
